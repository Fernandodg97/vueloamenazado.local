{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "a2dismod mpm_event mpm_worker 2>/dev/null || true && sed -i \"s/Listen 80/Listen ${PORT}/g\" /etc/apache2/ports.conf && find /etc/apache2/sites-enabled -name '*.conf' -exec sed -i \"s/*:80>/*:${PORT}>/g\" {} \\; && apache2-foreground",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
